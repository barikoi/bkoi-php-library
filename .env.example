# Barikoi API Configuration
# Get your API key from: https://barikoi.com

# Required for integration tests and production use
BARIKOI_API_KEY=your_real_api_key_here

# API base URL (default: https://barikoi.xyz)
BARIKOI_BASE_URL=https://barikoi.xyz

# Note:
# - Mock tests (126 tests) do NOT require a real API key
# - Integration tests (29 tests) REQUIRE a real API key
# - Production usage requires a real API key
#
# To run integration tests:
# BARIKOI_API_KEY=your_key composer test:integration

# Discord Test Notifications
# Get webhook URL from: Discord Server Settings > Integrations > Webhooks
# Create a webhook in your Discord channel and paste the URL here
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
DISCORD_NOTIFICATIONS_ENABLED=true

# CI/CD Environment
# Set to true when running in continuous integration (GitHub Actions, GitLab CI, etc.)
# This is automatically set by CI systems but can be set manually for local testing
CI=false
